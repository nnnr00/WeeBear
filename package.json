{
  "name": "telegram-vip-bot",
  "version": "1.0.0",
  "description": "Telegram VIP Bot with Admin Panel",
  "main": "api/webhook.js",
  "scripts": {
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.64.0",
    "@vercel/kv": "^1.0.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}
